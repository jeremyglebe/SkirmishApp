/// This service will replace DataService in V2 of the project, for now it is not in use.

import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class StorageService {
  constructor() {}
}
